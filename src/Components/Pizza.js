import React from 'react';

const Pizza = (props) => {

    return(
        <div>
            <form id="pizza-form">
                <label htmlFor="">Pizza</label>
                <input type="text" />
            </form>
        </div>
    )
}

export default Pizza;